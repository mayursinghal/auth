import React from 'react';
import { View, ActivityIndicator } from 'react-native';

const Spinner = () => (
        <View >
    <ActivityIndicator />
            </View>
    );

export { Spinner };
